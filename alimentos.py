def registrar_alimento(lista_comidas):
    alimento_consumido = input("Ingresa el alimento que consumiste este dia: ").lower()
    al = False
    for co in lista_comidas:
        if co["alimento"] == alimento_consumido:
            al = True
        if al == True
    
